services:
  - type: web
    name: a3h
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn erp.wsgi"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: django-db
          property: connectionString

databases:
  - name: django-db
  